import { ScrollArea } from "@/components/ui/scroll-area";

export const TodoTable = () => {
  return (
    <ScrollArea className="h-full w-full rounded-sm border-2 border-primary"></ScrollArea>
  );
};
